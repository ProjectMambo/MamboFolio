name: Deploy MamboFolio to Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout MamboFolio
        uses: actions/checkout@v4
        with:
          path: MamboFolio

      - name: Checkout MamboSite
        uses: actions/checkout@v4
        with:
          repository: ProjectMambo/MamboSite
          ref: 11b023d07eb56db8c64c831de0e3d6d387a9acb8
          path: MamboSite

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: 1.95.0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: |
            MamboSite/package-lock.json
            MamboFolio/package-lock.json

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Install MamboSite dependencies
        run: npm ci
        working-directory: MamboSite

      - name: Install MamboFolio dependencies
        run: npm ci
        working-directory: MamboFolio

      - name: Build static site with MamboSite
        run: >-
          cargo run --locked --quiet
          --manifest-path MamboSite/Cargo.toml
          --package mambosite-cli --
          --config MamboFolio/mambo.toml build

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: MamboFolio/out

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
